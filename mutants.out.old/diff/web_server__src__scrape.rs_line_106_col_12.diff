--- web_server/src/scrape.rs
+++ delete ! in parse_root_comments
@@ -98,17 +98,17 @@
         let is_root = tr
             .select(&ind_img_sel)
             .next()
             .and_then(|img| img.value().attr("width"))
             .and_then(|w| w.parse::<i32>().ok())
             .map(|w| w == 0)
             .unwrap_or(true); // default to true if missing
 
-        if !is_root {
+        if  /* ~ changed by cargo-mutants ~ */is_root {
             continue;
         }
 
         // Extract comment id from the age link (e.g., <span class="age"><a href="item?id=<COMMENT_ID>">...</a>)
         let id_opt = tr
             .select(&age_link_sel)
             .next()
             .and_then(|a| a.value().attr("href"))
